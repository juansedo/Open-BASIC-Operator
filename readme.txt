/**
* Este documento explicará como nos repartiremos el trabajo de lenguajes.
*/

Necesidades:

- (JuanSe) Implementación de Hash Table (String -> Float) para guardar variables
La Hash Table debe tener:
    - Método para analizar si ya existe la variable
- (Calle) Código de lectura de archivos por línea, dentro de un ciclo while
- (Calle) Código de comparación de líneas de código, que elimine los espacios en blanco iniciales
y permita definir si se está declarando, operando y asignando o es el llamado a una función 